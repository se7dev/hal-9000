initSidebarItems({"struct":[["GiveawayController","GiveawayController  Serves as the controller to organize giveaways in channels  Holds users as a HashSet and giveaway_running to check if giveaway has been started.  # Example  `fn choose_random_user() {          let mut users = GiveawayController { users: HashSet::new(), giveaway_running: true };          users.add_user(String::from(\"!giveawayenter basti\"));          users.add_user(String::from(\"!giveawayenter robin\"));          users.add_user(String::from(\"!giveawayenter ralph\"));          let chosen = users.choose_user();          assert!(!chosen.is_empty()); `"]]});